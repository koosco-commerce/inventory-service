apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service-mariadb
  namespace: commerce
  labels:
    app: inventory-service-mariadb
    service: inventory-service
spec:
  replicas: 1
  strategy:
    type: Recreate # Important for databases with persistent storage
  selector:
    matchLabels:
      app: inventory-service-mariadb
  template:
    metadata:
      labels:
        app: inventory-service-mariadb
        service: inventory-service
    spec:
      containers:
        - name: mariadb
          image: mariadb:11.8.5
          ports:
            - name: mysql
              containerPort: 3306
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: inventory-service-mariadb-secret
                  key: MYSQL_ROOT_PASSWORD
            - name: MYSQL_DATABASE
              value: "commerce-inventory"
            - name: MYSQL_USER
              value: "admin"
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: inventory-service-mariadb-secret
                  key: MYSQL_PASSWORD
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          volumeMounts:
            - name: mariadb-data
              mountPath: /var/lib/mysql
          livenessProbe:
            tcpSocket:
              port: 3306
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: 3306
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 1
            failureThreshold: 3
      volumes:
        - name: mariadb-data
          persistentVolumeClaim:
            claimName: inventory-service-mariadb-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service-mariadb
  namespace: commerce
  labels:
    app: inventory-service-mariadb
    service: inventory-service
spec:
  type: ClusterIP
  selector:
    app: inventory-service-mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
---
apiVersion: v1
kind: Secret
metadata:
  name: inventory-service-mariadb-secret
  namespace: commerce
  labels:
    app: inventory-service-mariadb
    service: inventory-service
type: Opaque
stringData:
  MYSQL_ROOT_PASSWORD: "root"
  MYSQL_PASSWORD: "admin1234"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: inventory-service-mariadb-pvc
  namespace: commerce
  labels:
    app: inventory-service-mariadb
    service: inventory-service
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  # storageClassName: standard # Uncomment and specify your storage class
